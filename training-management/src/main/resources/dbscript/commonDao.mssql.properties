CommonDao.findByYear=select e.status from tms_training_date e where date_part('year', e.training_date)=:year
CommonDao.findByStatusAndId=select e.status from tms_training_date e where e.status =:status and e.training_date_id!=:trainingDateId
CommonDao.findByYearAndId=select e.status from tms_training_date e where date_part('year', e.training_date)=:year and e.training_date_id!=:trainingDateId